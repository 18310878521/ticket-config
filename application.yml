
spring:
  profiles:
    active: dev

---
server:
  servlet:
    context-path: /
spring:
  profiles: prod
  application:
    name: ticket-prod

---
server:
  servlet:
    context-path: /
spring:
  profiles: test
  application:
    name: ticket-test

---
server:
  servlet:
    context-path: /
spring:
  profiles: dev
  application:
    name: ticket-dev